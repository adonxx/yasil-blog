# 🌟 新手超详细部署指南

## 你现在的情况
- ✅ 有 VS Code
- ✅ 有 Python (这个项目不需要)
- ✅ 会 Git 基本操作
- ✅ GitHub 仓库：adonxx.github.io/yasil66/

## 第0步：理解你的博客地址

你的 GitHub 用户名是 `adonxx`，所以：
- 主网站地址：https://adonxx.github.io/
- 这个博客地址：https://adonxx.github.io/yasil66/

## 第1步：下载项目文件

我已经为你创建了所有文件。你需要：

1. 在你的电脑上创建一个文件夹，比如 `D:\my-blog\`
2. 把我给你的所有文件放进去

文件列表：
```
my-blog/
├── _config.yml
├── _layouts/
│   ├── default.html
│   └── page.html
├── _posts/
│   └── 2025-02-12-welcome.md
├── assets/
│   ├── css/style.css
│   ├── js/main.js
│   └── images/ (放你的图片)
├── index.html
├── resources.md
├── travel.md
├── moments.md
├── tech.md
├── Gemfile
├── .gitignore
├── README.md
└── DEPLOYMENT.md
```

## 第2步：用 VS Code 打开项目

1. 打开 VS Code
2. 点击"文件" > "打开文件夹"
3. 选择你刚才创建的 `my-blog` 文件夹
4. 点击"选择文件夹"

## 第3步：连接到 GitHub

### 方式一：用 VS Code 图形界面 (推荐新手)

1. **检查 Git 是否已安装**
   - 在 VS Code 按 `Ctrl + `` (左上角的反引号键)
   - 输入：`git --version`
   - 如果显示版本号，说明已安装
   - 如果没有，去 https://git-scm.com/ 下载安装

2. **配置 Git 用户信息（首次使用）**
   在终端输入：
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

3. **初始化仓库**
   - 点击 VS Code 左侧的"源代码管理"图标（像树枝的图标）
   - 点击"初始化存储库"按钮

4. **提交文件**
   - 在顶部的输入框输入：`初始化博客`
   - 点击"✓ 提交"按钮
   - 如果弹出提示选"是"或"始终"

5. **连接远程仓库**
   在终端（Ctrl + `）依次输入：
   ```bash
   git remote add origin https://github.com/adonxx/yasil66.git
   git branch -M main
   git push -u origin main
   ```

6. **输入 GitHub 凭据**
   - 第一次推送会要求登录
   - 用户名：`adonxx`
   - 密码：使用 Personal Access Token（不是 GitHub 密码）

#### 🔑 如何获取 Personal Access Token？

1. 登录 GitHub
2. 点击右上角头像 > Settings
3. 左侧滚动到最下面 > Developer settings
4. Personal access tokens > Tokens (classic)
5. Generate new token (classic)
6. Note 写：`Blog deployment`
7. 勾选 `repo` (完整勾选)
8. 点击最下面的 Generate token
9. **复制生成的 token（只显示一次！）**
10. 粘贴作为密码使用

### 方式二：用命令行

如果你习惯用命令行，在终端执行：

```bash
# 1. 初始化仓库
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "初始化博客"

# 4. 添加远程仓库
git remote add origin https://github.com/adonxx/yasil66.git

# 5. 推送
git branch -M main
git push -u origin main
```

## 第4步：启用 GitHub Pages

1. 打开浏览器，访问：https://github.com/adonxx/yasil66
2. 点击顶部的 **Settings** (设置)
3. 在左侧菜单找到 **Pages**
4. 在 "Source" 部分：
   - Branch: 选择 `main`
   - Folder: 选择 `/ (root)`
5. 点击 **Save**

⏰ **等待 2-5 分钟**，GitHub 会自动构建你的网站

## 第5步：查看你的网站

在浏览器访问：**https://adonxx.github.io/yasil66/**

🎉 恭喜！你的博客已经上线了！

## 第6步：更换首页背景图

### 简单方法：使用在线图片

1. 找一张你喜欢的图片，上传到图床（比如：https://imgbb.com/）
2. 复制图片链接
3. 在 VS Code 打开 `assets/css/style.css`
4. 按 `Ctrl + F` 搜索：`.hero-section`
5. 找到第 114 行左右，修改：

```css
.hero-section {
    height: 100vh;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    /* 下面这一行改成你的图片链接 */
    background-image: url('https://你的图片链接.jpg');
    background-size: cover;
    background-position: center;
}
```

6. 删除或注释掉后面的：
```css
/* 注释掉这个渐变背景
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
*/
```

### 本地图片方法：

1. 把你的图片（建议命名为 `hero-bg.jpg`）复制到 `assets/images/` 文件夹
2. 修改 CSS：
```css
background-image: url('../images/hero-bg.jpg');
```

### 提交更新：

在 VS Code 终端：
```bash
git add .
git commit -m "更新背景图"
git push
```

等待几分钟后刷新网站查看！

## 第7步：日常更新流程

每次修改网站后：

1. **保存文件** (Ctrl + S)
2. **提交更改**：
   ```bash
   git add .
   git commit -m "描述你的更改"
   git push
   ```
3. **等待 2-3 分钟**
4. **刷新网站**查看效果

## 常见问题解决

### 问题1：git 命令不识别

**解决**：安装 Git
- 访问：https://git-scm.com/
- 下载安装
- 重启 VS Code

### 问题2：推送时要求密码

**解决**：使用 Personal Access Token（见上面第3步）

### 问题3：网站打开是 404

**解决**：
1. 检查 `_config.yml` 中的 `baseurl: "/yasil66"`
2. 确认仓库名是 `yasil66`（不是 `adonxx.github.io`）
3. 确认 GitHub Pages 已启用（Settings > Pages）

### 问题4：样式没有加载

**解决**：
1. 按 Ctrl + Shift + R 强制刷新
2. 等待 5 分钟让 GitHub Pages 完全部署
3. 检查浏览器控制台（F12）是否有错误

### 问题5：想在本地预览

**解决**：需要安装 Jekyll（可选，不是必须的）

Windows 用户：
1. 安装 Ruby：https://rubyinstaller.org/
2. 选择 Ruby+Devkit 3.2.X (x64)
3. 安装时勾选"Add Ruby to PATH"
4. 打开新的命令提示符，输入：
   ```bash
   gem install bundler jekyll
   cd 你的博客文件夹路径
   bundle install
   bundle exec jekyll serve
   ```
5. 访问：http://localhost:4000/yasil66/

## 🎯 快速参考

### 修改标题和签名
编辑 `_config.yml`：
```yaml
title: 你的博客名
description: 你的个性签名
```

### 修改导航栏
编辑 `_layouts/default.html`（第 18-24 行左右）

### 修改颜色主题
编辑 `assets/css/style.css`（第 2-12 行的变量）

### 添加新文章
在 `_posts` 文件夹创建：`YYYY-MM-DD-标题.md`

## 📞 需要帮助？

- GitHub Pages 文档：https://docs.github.com/cn/pages
- Jekyll 文档：https://jekyllrb.com/
- 或者直接问我！

祝你玩得开心！🎉
